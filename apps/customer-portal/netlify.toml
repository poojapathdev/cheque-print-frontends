[build]
  command   = "npx turbo build --filter=customer-portal"
  publish   = "apps/customer-portal/dist"

[build.environment]
  NODE_VERSION = "20"

[build.ignore]
  command = "git diff --quiet HEAD^ HEAD -- apps/customer-portal/ packages/"
