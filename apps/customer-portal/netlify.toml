[build]
  command   = "npx turbo build --filter=customer-portal"
  publish   = "apps/customer-portal/dist"
  ignore  = "git diff --quiet HEAD^ HEAD -- apps/customer-portal/ packages/"

[build.environment]
  NODE_VERSION = "22"
