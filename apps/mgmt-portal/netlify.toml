[build]
  command   = "npx turbo build --filter=mgmt-portal"
  publish   = "apps/mgmt-portal/dist"

[build.environment]
  NODE_VERSION = "20"

[build.ignore]
  command = "git diff --quiet HEAD^ HEAD -- apps/mgmt-portal/ packages/"
